#нужно синхронизировать ветки уделенного и локального репозитроия

  git branch -d $(git branch -vv  | grep ': gone]' | cut -d' ' -f3)
  // Это удалит удаленные локальные ветви, но оставит те, которые еще не объединены.
  //Вместо этого используйте -D, если они вам тоже не нужны.
